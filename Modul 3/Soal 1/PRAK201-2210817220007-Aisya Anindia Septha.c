#include <stdio.h>
int main ()
{
char a, b, c, d, e, f, g;
printf("Nama                    :");
gets(&a);
printf("NIM                     :");
gets(&b);
printf("Kelas Paralel           :");
gets(&c);
printf("Tempat/Tanggal Lahir    :");
gets(&d);
printf("Alamat                  :");
gets(&e);
printf("Hobby                   :");
gets(&f);
printf("No. HP                  :");
gets(&g);
}
